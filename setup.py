from setuptools import find_packages, setup

setup(
    name='kaya',
    packages=find_packages(),
    version='0.1.0',
    description='Package which implements the kaya identity',
    author='MikeySaw',
    license='',
)